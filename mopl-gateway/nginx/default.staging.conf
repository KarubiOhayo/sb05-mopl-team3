server {
    listen 80;
    server_name _;

    # Frontend (SPA) 정적 파일 서빙
    root /usr/share/nginx/html;
    index index.html;

    location / {
        # SPA 라우팅 지원
        try_files $uri $uri/ /index.html;
    }
}
